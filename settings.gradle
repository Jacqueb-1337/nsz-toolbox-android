pluginManagement {
    repositories {
        gradlePluginPortal()
        google()
        mavenCentral()
    }

    plugins {
        id "org.jetbrains.kotlin.android" version "1.9.10"
    }
}

rootProject.name = "nsz-toolbox-android"
include(":app")